// function
    // two params: array of Persons, size of array
    // returns standard deviation of all ages

// squareRoot ( sum( (X_i - avg)^2) ) / N ) 

int stdDev()
{
    int stdDev = 0;

    return stdDev;
}